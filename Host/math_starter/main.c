#include <stdlib.h>
#include <stdio.h>
#include "math.h"

int main()
{
	printf("Sum = %d\n", add(4, 6));
	printf("difference = %d\n", subtract(4, 6));
	printf("product = %d\n", multiply(4, 6));
	printf("quotient = %f\n", divide(4, 6));

	return 0;
}
